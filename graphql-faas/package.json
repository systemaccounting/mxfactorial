{
  "name": "graphql-faas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "zip": "zip -r lambda.zip index.js src node_modules package.json package-lock.json .gitignore",
    "update:faas:prod": "aws lambda update-function-code --function-name mxfactorial-graphql-server-prod --zip-file fileb://$(pwd)/lambda.zip",
    "update:faas:dev": "aws lambda update-function-code --function-name mxfactorial-graphql-server-dev --zip-file fileb://$(pwd)/lambda.zip --region us-east-2",
    "deploy:prod": "yarn run zip && yarn run update:faas:prod",
    "deploy:dev": "yarn run zip && yarn run update:faas:dev",
    "cp:lambda": "echo \"copying over to terraform\" && cp lambda.zip ../terraform/workspaces/lambda.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.325.0",
    "aws-serverless-express": "^3.3.5",
    "body-parser-graphql": "^1.1.0",
    "express": "^4.16.3",
    "express-graphql": "^0.6.12",
    "graphql": "^14.0.2",
    "uuid": "^3.3.2"
  }
}