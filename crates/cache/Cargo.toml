[package]
name = "cache"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
description = "trait for cache tools"

[dependencies]
async-trait = "0.1.73"
thiserror = "2.0"
types = { path = "../types" }
