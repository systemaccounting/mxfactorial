version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.11.0-stretch-browsers-legacy
    working_directory: ~/repo
    steps:
      - checkout

      - run: cd react
      - run: npm install

      - run: npm test