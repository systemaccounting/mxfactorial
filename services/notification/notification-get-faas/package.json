{
  "name": "notification-get-faas",
  "version": "1.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "test": "env-cmd jest --silent --verbose",
    "test:integration": "env-cmd jest --detectOpenHandles --runInBand --silent --verbose --testRegex='.*\\.integ\\.[tj]sx?'"
  },
  "devDependencies": {
    "aws-sdk": "^2.540.0",
    "env-cmd": "^10.0.1",
    "jest": "^24.9.0"
  }
}
